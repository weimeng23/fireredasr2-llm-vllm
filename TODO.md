# 待办

- [ ] 接近切片长度上限时，优先寻找短静音切点（暂缓实施，独立于队列与片段调度改造）。
  - 保留短停顿位置，使用独立于 `VAD_SILENCE_MS` 的切点静音阈值；自然断句仍使用当前 1500 ms 阈值。
  - 在 `CHUNK_SECONDS` 上限之前的有限窗口内寻找合适停顿，找不到时按上限强切，保证最终片段不超过上限。
  - 搜索窗口和短静音阈值待验证；此前讨论的 25～30 秒窗口及 200 ms 停顿仅作为候选值。
  - 验证切片之间不丢失或重复音频、原始时间戳正确，以及有无停顿和音频尾段等边界情况。

当前仍采用 VAD 自然分段后按最长 30 秒强切的策略。
